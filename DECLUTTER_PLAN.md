# Declutter Plan

Plan date: 2026-04-21

## A. Safe Delete Now

| Item | Why It Is Large | Required? | Safe To Remove? | Estimated Savings | Risk |
| --- | --- | --- | --- | ---: | --- |
| `.next.zip` | Archived generated Next.js output | No | Yes | 1.25 GiB | Low |
| `.next` | Next.js build/dev output, especially Turbopack cache | No | Yes | 1.08 GiB | Low |
| `dist` | Old Vite/React production build with duplicated assets | No | Yes | 5.38 MiB | Low |
| `node_modules\.vite` | Vite cache left from previous React/Vite workflow | No | Yes | 39.15 MiB | Low |
| `nul` | Accidental empty Windows artifact | No | Yes | 0 B | Low |
| Empty `app\debug` directory | Empty folder | No | Yes | 0 B | Low |

## B. Delete Only After Verifying Unused

| Item | Why It Is Large/Relevant | Required? | Safe To Remove? | Estimated Savings | Risk |
| --- | --- | --- | --- | ---: | --- |
| `node_modules` | Installed dependencies, including Next native compiler and large packages | Not source, but required for immediate local commands | Optional only | 516.56 MiB | Medium |
| `src\Component\Route\Router.jsx` | Old React Router route table; not referenced by Next pages found in audit | Probably not | Verify before source cleanup | 2.26 KiB | Medium |
| `compat\react-router-dom.js` | Migration shim for `Link`, `NavLink`, `useNavigate` | Yes, currently imported | No, unless all imports are refactored | 1.45 KiB | High |
| `react-icons` dependency | Installed package is 82.20 MiB and creates large generated chunks | Yes, currently imported broadly | Only after replacing icons/import strategy | Up to 82.20 MiB installed | High |
| migration/performance markdown files | Old audit/planning documentation | Team-dependent | Optional | about 28 KiB | Low |
| duplicate tiny `halkhata.png` copies | Same hash appears in `src\assets`, `src\assets\Busniess`, and `public` | Some copies may be used by different contexts | Verify import/public usage first | less than 3 KiB | Low |
| large source images under `src\assets` | Active UI imagery; largest is `Busniess\grocery.png` at 1.69 MiB | Yes | Optimize, do not delete | Potentially 1-3 MiB | Medium |

## C. Keep

| Item | Why Keep | Risk If Removed |
| --- | --- | --- |
| `app` | Active Next.js pages, layouts, loading/not-found routes, and API routes | App routes/API break |
| `src\Component`, `src\components`, `src\context`, `src\config`, `src\hooks`, `src\lib` | Active application source imported by `app` routes | UI/auth/API flows break |
| `src\assets` | Active imported images | Pages fail to build or lose imagery |
| `public` | Manifest, icons, static JSON and public assets | Public files missing |
| `supabase` | SQL setup/migration scripts | Database setup references lost |
| `package.json` and `package-lock.json` | npm dependency manifest and lockfile | Install/build reproducibility lost |
| `next.config.mjs`, `postcss.config.mjs`, `eslint.config.js`, `tsconfig.json`, `next-env.d.ts` | Active Next.js, Tailwind, ESLint, TypeScript configuration | Tooling/build behavior breaks |
| `.env.example` | Environment documentation | Onboarding/config clarity reduced |
| `.git` | Git history and metadata | Repository damaged |

## Cleanup Execution Order

1. Remove `.next.zip`.
2. Remove `.next`.
3. Remove `dist`.
4. Remove `node_modules\.vite` only, keeping the rest of `node_modules`.
5. Remove `nul` and empty `app\debug`.
6. Update `.gitignore` with broader generated/cache coverage.
7. Run a build verification with existing `node_modules`.
8. Remove any `.next` output generated by verification so the repo ends clean.
9. Re-measure size and write `DECLUTTER_SUMMARY.md`.

